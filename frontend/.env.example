# Thermo-Shield Environment Configuration
# Default to mock simulation engine (pure frontend prototype)
VITE_USE_MOCK_ENGINE=true

# API Base URL for future backend simulation solver (e.g. EnergyPlus / OpenBPS / Flask service)
VITE_API_BASE_URL=
